// TypeScript uses this file for type checking; Metro resolves the .native or .web implementation at runtime.
import type { BiometricService } from './biometrics.types';

export const biometrics = null as unknown as BiometricService;
